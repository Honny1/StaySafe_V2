<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pc.staysafe">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LogoActivity"
            android:theme="@style/FullscreenTheme1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".DangerActivity" />
        <activity android:name=".TestActivity" />
        <activity android:name=".Login" />
        <activity android:name=".AvoidInternetActivity" />
        <activity android:name=".AvoidReallifeActivity" />
        <activity android:name=".Register" />
        <activity android:name=".DangerActivity1" />
        <activity android:name=".DangerActivity2" />
        <activity android:name=".DangerActivity3"></activity>
    </application>

</manifest>